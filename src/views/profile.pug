doctype html
head
	link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css.bootstrap.min.css")
	link(rel="stylesheet" href="stylesheet.css" type="text/css")
body.background
		if user
			div.text3
				h2 welcome #{user.email} to your profile page
				h3 your password is #{user.password}
				a(href='/logout') click here to logout
		else
			div.text3
				p you must enter your login info first!
				a(href='/register')
